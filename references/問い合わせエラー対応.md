# Query Error Handling

## 分けて扱うべき失敗

- not found
- forbidden
- duplicate
- validation 済みでも保存できないケース
- unexpected error

## 指針

- not found と forbidden を同じ null にしない
- DB error をそのまま user に返さない
- route 層で HTTP status に変換する
- log に必要な情報だけ残し、秘密情報は出さない
